# Electron Builder Environment Configuration
# This file contains environment variables needed for building the Electron app

# Set the app category for macOS
APPLE_CATEGORY=public.app-category.developer-tools

# Enable code signing (set to false for development builds)
CSC_IDENTITY_AUTO_DISCOVERY=false

# Skip notarization for development (set to true for production)
APPLE_SKIP_NOTARIZATION=true

# Build output directory
BUILD_OUTPUT_DIR=release

# Enable source maps for debugging
DEBUG=electron-builder

# Set the target platform (uncomment as needed)
# ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES=true